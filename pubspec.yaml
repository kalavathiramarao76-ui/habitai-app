name: habit_coach
description: "AI-Powered Habit Coach & Streak Tracker — Build better habits with AI."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.0
  fl_chart: ^0.69.0
  intl: ^0.19.0
  uuid: ^4.4.0
  flutter_local_notifications: ^18.0.0
  google_fonts: ^6.2.0
  http: ^1.2.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
